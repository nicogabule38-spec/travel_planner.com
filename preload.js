// Minimal preload script. Add secure APIs here if needed.
// Running with contextIsolation=true; no node globals are exposed to the renderer.

window.addEventListener('DOMContentLoaded', () => {
  // Placeholder for future preload tasks
});
